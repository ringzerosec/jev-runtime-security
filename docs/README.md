# docs

- `integrations.md` — the delivery contract for trace records: event list,
  schema, HMAC signing, redaction, verdict hooks, and a curl-able receiver.

The trace format itself is documented in `trace/README.md`, with a JSON Schema
at `trace/schema/v1.json`.
